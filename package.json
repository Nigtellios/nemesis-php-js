{
  "name": "nemesis",
  "version": "0.0.1",
  "description": "",
  "keywords": "front-end",
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build src/*",
    "dev": "parcel src/index.html --open -p 2137",
    "start": "yarn build && yarn dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "watchreload": {
    "files": ["**/*.php", "**/*.svg"]
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-watch-reload": "^1.0.3"
  },
  "dependencies": {
    "normalize": "^0.3.1",
    "sass": "^1.49.0"
  }
}
